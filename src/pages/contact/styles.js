const contactStyles = theme => ({
  container: {
    ...theme.container,
    paddingTop: 30
  },
  anchor: {
    color: theme.palette.primary.main,
    fontSize: '1em'
  }
})

export default contactStyles
